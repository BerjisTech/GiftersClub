# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
# org.gradle.parallel=true
# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Automatically migrate existing third-party libraries to AndroidX using Jetifier
android.enableJetifier=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true

ROLLBAR_CLIENT_TOKEN=ef7c60872c014b5aaa87b7f2565d73bf
ROLLBAR_ENV=production

# Java toolchain configuration
# Cross-platform note:
# - Do NOT set org.gradle.java.home in this repository (use ~/.gradle/gradle.properties if needed).
# - Gradle toolchains + auto-download will provision a matching JDK per dev machine.
# - Android Gradle Plugin 8.x requires JDK 17; the app module declares 17 for compileOptions/kotlinOptions.
org.gradle.java.installations.auto-download=true

# Workaround: Allow Android plugin to write artifacts outside build/outputs when
# Android Studio injects a custom APK location (e.g., app/release). Preferred is
# to use default output locations and post-copy artifacts, but enabling this flag
# unblocks builds if the IDE injects android.injected.apk.location.
android.overridePathCheck=true
